
class CardGame:


def check_for_ace(self, card):
    if card.value = 1: # missing '='
        return True
    else                # missing ':' after else
        return False


dif highest_card(self, card1 card2): # 'dif' syntax error | missing ',' after 'card1'
    if card1.value > card2.value:
        return card                 # 'card' syntax error, instead of 'card1'
    else:
        return card2



def cards_total(self, cards):  
    total           # initialize 'total' variable
    for card in cards:
        total += card.value
        return "You have a total of" + total # return indented incorrectly || need to convert 'total' to string to use '+'
